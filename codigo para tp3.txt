emp_new_parser(char *a, char *b, char *c, char *d)
{
	int id;
	int horas;
	int sueldo;
	*employee aux;	

	id=atoi(a);
	
	horas=atoi(c);
	sueldo=atoi(d);
		
	emp_new();	

	emp_setID

}

emp_setID()
{
	if (id>0)
	{
		
		
	}


}



para el punto 1, se tiene que hacer el parser con todo tipo %s
para el 2 se puede

sprintf(var1,"%d", VarQueTiraElDatoEntero);